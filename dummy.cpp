// A dummy C++ file that doesn't contain any code.
// Adding it to Scons' list of source files for a target will force it to be linked using g++ rather than gcc
// This is necessary in order to avoid linker errors when linking a C program with static libraries that contain C++ code.

